/**
 * @file config.js
 * @version 1.0.0
 */

const config = {
    audio: {
        mute: false,
        volume: 1,
    },
    debug: {
        preventCaching: false,
    },
    maxTimestep: 0.1,
    renderer: {
        antialias: true,
    },
};

export default config;
